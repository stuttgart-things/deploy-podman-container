---
- hosts: all
  become: true
  roles:
    # - base-os-setup
    # - install-configure-podman

    - role: deploy-podman-container
      vars: 
        container_name: ende4
        container_image: nginx:latest
        mounts:
          webserverconfig:
            host: /tmp/test
            container: test:Z
        ports:
          http:
            host: 8080
            container: 80
          https:
            host: 8443
            container: 443  
        container_run_args: >-
          -d
        #container_cmd_args: 